{
  "name": "poker-lib",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "./node_modules/typescript/bin/tsc && yarn run copy && yarn run build-enums",
    "build-enums": "node ./build-enums.js",
    "start": "yarn run build && node build/index.js",
    "copy": "cp ../poker-lib-wasm.* ./build/lib && cp ../poker-lib-wasm.* ./src/lib && cp ./src/lib/*.ts /poker/build/poker-lib",
    "test": "yarn run copy && mocha --timeout 150000 -r ts-node/register src/**/*.test.ts"
  }, 
  "author": "",
  "dependencies": {
    "ethers": "^5.1.0"
  },
  "devDependencies": {
    "@types/chai": "^4.2.21",
    "@types/mocha": "^9.0.0",
    "@types/node": "^16.7.6",
    "chai": "^4.3.4",
    "mocha": "^9.1.1",
    "ts-node": "^10.2.1",
    "typescript": "4.2.4"
  }
}
